#include <iostream>

using namespace std;

int main()
{
    cout<<"1 - >> mean input << mean output"<<endl;
    cout<<"2 - take care!! of <= that's true =< that's Error"<<"\n"<<"          >= that's true , but => that's Error in C++"<<endl;
    cout<<"3 - int = 1,2,3,4..... char = 'A', 'B', 'z', 'b' , @ # % ^..... float = 1.2, 3/6 0.0.... "<<endl;
    cout<<"4 - a % 2 is equal to a%2!=0"<<endl;
    cout<<"5 - a+=1 is equal to a = a + 1"<<endl;
    cout<<"5 - a*=a+b is equal to a = a * a + a * b or equal to  a = a( a + b)"<<endl;
    cout<<"6 - to output the \"in black board you should write it like \"  this open the code to understand"<<endl;
    cout<<"7 - cout<<\"\\ it will output like \ ..But cout<< \"// the out put is \\ "<<endl;
     cout<<"8 - You should know this (==) equal sign with \"IF" " "<<endl;
      cout<<"9 - sometime we use \"do while\" nstead of \"For\" thatuf we didn't know the number of loops " <<endl;
    return 0;
}
